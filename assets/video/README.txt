Drop these files here (exact names, referenced from index.html):
  fold-switch-ensemble.mp4
  binder-design.mp4
Keep clips short (<8s loop) and small (<5MB) so the page stays fast.
You can delete this README.txt once the real videos are in place.
